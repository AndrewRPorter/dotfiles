# dotfiles

Dotfiles to sync my local environment with [codespaces](https://github.com/features/codespaces).

## Install

```bash
git clone https://github.com/AndrewRPorter/dotfiles.git ~/dotfiles && ~/dotfiles/install
```
