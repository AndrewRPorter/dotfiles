set -g mouse on

# Split panes with | and -
bind | split-window -h
bind - split-window -v

# New window with n
bind n new-window

# Cmd+C / Cmd+V (requires terminal to send Cmd as Meta)
bind-key -n M-c copy-mode
bind-key -n M-v paste-buffer
